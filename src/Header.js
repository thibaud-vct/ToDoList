const Header = (props) => {
    return (
        <div className="header">
            <button>List</button>
            <span>ToDo List</span>
            <button>Login</button>
        </div>
    );
};
export default Header;
